# Sourced by non-interactive bash shells via BASH_ENV (set by the
# arc-toolchain kit) so mise shims — bun, node — are on PATH even for
# `bash -c` invocations that skip ~/.bashrc.
export PATH="$HOME/.local/share/mise/shims:$PATH"
